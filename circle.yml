machine:
  environment:
    LANG: en_US.UTF-8
  xcode:
    version: "9.0"
test:
  override:
    - set -o pipefail &&
      xcodebuild
        -project MCTObjectStore.xcodeproj
        -scheme MCTObjectStore
        -sdk iphonesimulator
        -destination 'id=0C2F39AB-0DBE-4577-8534-C51476C24A47'
        clean
        test
      | xcpretty -c
