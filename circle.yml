machine:
  environment:
    LANG: en_US.UTF-8
  xcode:
    version: "6.3.1"
test:
  override:
    - set -o pipefail &&
      xcodebuild
        -project MCTObjectStore.xcodeproj
        -scheme MCTObjectStore
        -sdk iphonesimulator test
      | xcpretty -c
